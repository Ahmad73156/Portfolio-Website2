// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    turbo: {
      // You can specify options if needed; keep empty if none
    },
  },
};

module.exports = nextConfig;
